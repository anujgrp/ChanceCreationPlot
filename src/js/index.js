var FIELD = "#field";
$(document).ready(function() { 
    //initialize everything
    function init() {
        //draw field
        drawField();
        //bring in the data
        readData();
    }
    init();
});